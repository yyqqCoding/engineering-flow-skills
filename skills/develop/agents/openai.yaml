interface:
  display_name: "Develop"
  short_description: "Align, approve, implement, and verify a change"
  default_prompt: "Use $engineering-flow:develop to align this change, pause for my approval, then implement it with focused verification."
policy:
  allow_implicit_invocation: false
